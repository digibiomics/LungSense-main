# Prefer this single URL (recommended)
# DATABASE_URL=postgresql+psycopg2://<DB_USER>:<DB_PASS>@localhost:5432/<DB_NAME>

# Optional (used if code reads individual values)
DB_HOSTNAME=localhost
DB_PORT=5432
DB_NAME=LungSense
DB_USERNAME=postgres
DB_PASSWORD=eshaal

# For Alembic / migrations (optional)
# ALEMBIC_DATABASE_URL=postgresql+psycopg2://<DB_USER>:<DB_PASS>@localhost:5432/<DB_NAME>

# App secrets
SECRET_KEY=devsecret
# SENTRY_DSN=